function Item_Stats_Remove(){
	var _lookup_type = argument[0];//Lookup type for character stats (if provided)
	var _character = argument[1];//Character (if provided)
	
	item_icon = spr_Remove_Icon;
	frame_number = 0;
}